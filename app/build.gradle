plugins { id 'com.android.application' }

android {
    namespace 'sa.techlight.printersetup'
    compileSdk 35

    defaultConfig {
        applicationId 'sa.techlight.printersetup'
        minSdk 23
        targetSdk 35
        versionCode 6
        versionName '1.5.0'
    }

    sourceSets {
        main {
            manifest.srcFile '../printer_setup/app/src/main/AndroidManifest.xml'
            java.srcDirs = ['../printer_setup/app/src/main/java']
            res.srcDirs = ['../printer_setup/app/src/main/res']
        }
        test.java.srcDirs = []
        androidTest.java.srcDirs = []
    }
}
